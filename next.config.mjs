/** @type {import('next').NextConfig}  */
const nextConfig = {
    images: {
        domains: ["www.furniro.shop", "encrypted-tbn2.gstatic.com", "cdn0.divan.by", "encrypted-tbn0.gstatic.com", "encrypted-tbn1.gstatic.com", "encrypted-tbn3.gstatic.com"],
      },
};

export default nextConfig;